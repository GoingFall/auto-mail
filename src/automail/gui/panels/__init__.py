"""各标签页。

每个面板只做三件事：把 ``AppState`` 的数据画出来、把用户的动作转成调用、
暴露一个 ``refresh()`` 供主窗口在任务结束后调用。

**面板里不写业务逻辑**：数据整形在 :mod:`automail.gui.viewmodels`，
后台线程与并发在 :mod:`automail.gui.worker`。这样面板几乎只剩布局代码，
而布局出问题一眼就能看见，不像逻辑错误那样隐蔽。
"""
